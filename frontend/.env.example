# API Configuration
# For local development (backend on 8081)
NEXT_PUBLIC_API_URL=http://localhost:8081

# Vercel / Docker production build: where Next.js proxies /api (see next.config.ts rewrites).
# Override if your Render service URL changes.
# BACKEND_PROXY_ORIGIN=https://prayer-chat-backend-web-service.onrender.com

# Billing / Stripe UI (must match backend APP_BILLING_ENABLED). When false, checkout and portal are hidden.
NEXT_PUBLIC_BILLING_ENABLED=false

# Production browser API base is window.location.origin (same host as the page, /api rewrite on Vercel).
# Set NEXT_PUBLIC_API_URL for local dev (above) or optional SSR; do not set apex in env while users use www (or vice versa).
#
# Legacy direct API host (cross-origin; avoid for cookie auth):
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# Google OAuth - required for "Sign in with Google" (same as backend GOOGLE_CLIENT_ID)
# Get from Google Cloud Console: APIs & Services > Credentials > your Web client
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional: Default Chatbot ID
NEXT_PUBLIC_DEFAULT_CHATBOT_ID=1
