# Allow install with peer dependency conflicts (e.g. @visx/pattern expects React 18; we use React 19).
# Required for npm ci in CI and for npm install without --legacy-peer-deps.
legacy-peer-deps=true
